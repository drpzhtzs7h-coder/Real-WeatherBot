# Real-WeatherBot
The version Technosheen/Ventry089 use on their own computer; taken directly by honeypotting with their infected version. Remove .venv if exist within (both prompt injections). Change env var WALLET as the monkeypatch exists in this version.
